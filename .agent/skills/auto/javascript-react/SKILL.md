---
name: javascript-react
description: "Javascript React for instaroadtax. 1 conventions, 5 fixes."
domain: javascript-react
composesFrom:
  - react
triggers:
  - glob: "**/*.jsx"
autoGenerated: true
enabled: true
---

# Javascript React

Auto-compiled from **14 real patterns** in **instaroadtax**. This skill is auto-routed to agents when working on javascript-react files.

## 🔧 Problem Playbooks

### Fixed null crash in Link — parallelizes async operations for speed
- import { useNavigate, Link, useLocation } from "react-router-dom";
+ import { useNavigate, Link, useLocation } from "react-router-dom";
- const BACKEND_URL = process.env.REACT_APP_BACKEND_URL || "http://127.0.0.1:8001";
+ const BACKEND_URL = process.env.REACT_APP_BACKEND_URL || "http://127.0.0.1:8001";
- const API = `${BACKEND_URL}/api`;
+ const API = `${BACKEND_URL}/api`;
- const LOGO_URL = 

**Actionable Steps:**
1. Modified 1 files
2. identifier: Link
3. identifier: API
4. identifier: QuotationDialog
5. identifier: InquiryDetailsDialog

### Fixed null crash in Link — parallelizes async operations for speed
- import { useNavigate, Link, useLocation } from "react-router-dom";
+ import { useNavigate, Link, useLocation } from "react-router-dom";
- const BACKEND_URL = process.env.REACT_APP_BACKEND_URL || "http://127.0.0.1:8001";
+ const BACKEND_URL = process.env.REACT_APP_BACKEND_URL || "http://127.0.0.1:8001";
- const API = `${BACKEND_URL}/api`;
+ const API = `${BACKEND_URL}/api`;
- const LOGO_URL = 

**Actionable Steps:**
1. Modified 1 files
2. identifier: Link
3. identifier: API
4. identifier: QuotationDialog
5. identifier: InquiryDetailsDialog

### Patched security issue Link
-               <Link to="/check-status" className="w-full">
+               <Link to={`/check-status?inquiryId=${encodeURIComponent(inquiryId)}`} className="w-full">
-                 <Button className="w-full bg-indigo-500 hover:bg-indigo-600 text-white" data-testid="btn-check-status">
+                 <Button className="w-full bg-indigo-500 hover:bg-indigo-600 text-white" data-testid="btn-ch

**Actionable Steps:**
1. Modified 1 files
2. identifier: Link
3. identifier: Button
4. identifier: Check
5. identifier: Status

### Fixed null crash in Link — prevents null/undefined runtime crashes
- import { useState } from "react";
+ import { useEffect, useState } from "react";
- import { Link } from "react-router-dom";
+ import { Link, useLocation } from "react-router-dom";
- const CheckStatus = () => {
+ const CheckStatus = () => {
-   const [inquiryId, setInquiryId] = useState("");
+   const location = useLocation();
-   const [loading, setLoading] = useState(false);
+   const [inq

**Actionable Steps:**
1. Modified 1 files
2. identifier: Link
3. identifier: CheckStatus
4. identifier: Please
5. identifier: API

### Fixed null crash in Link — parallelizes async operations for speed
- import { useNavigate, Link, useLocation } from "react-router-dom";
+ import { useNavigate, Link, useLocation } from "react-router-dom";
- const BACKEND_URL = process.env.REACT_APP_BACKEND_URL || "http://127.0.0.1:8001";
+ const BACKEND_URL = process.env.REACT_APP_BACKEND_URL || "http://127.0.0.1:8001";
- const QuotationDialog = ({ inquiry, open, onClose, onSubmit }) => {
+ const QuotationDial

**Actionable Steps:**
1. Modified 1 files
2. identifier: Link
3. identifier: QuotationDialog
4. identifier: InquiryDetailsDialog
5. identifier: Dialog

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Fixed null crash in Link — parallelizes async operations for speed — confirmed 5x** — - import { useNavigate, Link, useLocation } from "react-router-dom";
+ import { useNavigate, Link, 

## 🤔 Decisions & Trade-offs

- **trade-off in PaymentResult.jsx** — File updated (external): frontend/src/pages/PaymentResult.jsx

Content summary (132 lines):
import {
- **trade-off in PaymentPage.jsx** — File updated (external): frontend/src/pages/PaymentPage.jsx

Content summary (201 lines):
import { u
- **trade-off in CheckStatus.jsx** — File updated (external): frontend/src/pages/CheckStatus.jsx

Content summary (196 lines):
import { u
- **trade-off in AdminLogin.jsx** — File updated (external): frontend/src/pages/AdminLogin.jsx

Content summary (120 lines):
import { us

---
*Auto-generated by BrainSync 🧠 | 14 patterns | 2026-03-28*
